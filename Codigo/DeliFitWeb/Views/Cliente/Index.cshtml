@model IEnumerable<DeliFitWeb.Models.ClienteViewModel>

@{
    ViewData["Title"] = "Gestão de Clientes";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold">Clientes</h2>
            <p class="text-muted">Gerencie a base de usuários cadastrados na plataforma DeliFit.</p>
        </div>
    </div>

    <div class="row mb-4 align-items-center">
        <div class="col-md-6">
            <a href="javascript:history.back()" class="btn btn-sm btn-outline-secondary px-3">
                <i class="bi bi-chevron-left"></i> Voltar
            </a>
        </div>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table id="clientesTable" class="table table-hover align-middle mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="ps-4 py-3">ID</th>
                            <th class="py-3">Cliente</th>
                            <th class="py-3">E-mail</th>
                            <th class="py-3">Telefone</th>
                            <th class="text-center py-3">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td class="ps-4 fw-bold text-muted">#@item.Id</td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="avatar-sm bg-light-orange rounded-circle me-3 d-flex align-items-center justify-content-center">
                                                <i class="bi bi-person-fill text-orange"></i>
                                            </div>
                                            <span class="fw-bold text-dark">@item.Nome</span>
                                        </div>
                                    </td>
                                    <td class="text-muted">@item.Email</td>
                                    <td class="text-muted">@item.Telefone</td>
                                    <td class="text-center">
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-orange-outline px-3">
                                            Visualizar Cadastro
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" class="text-center py-5">
                                    <i class="bi bi-people text-muted opacity-25" style="font-size: 3rem;"></i><br />
                                    <span class="text-muted">Nenhum cliente encontrado na base de dados.</span>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {

    <script>
        $(document).ready(function () {
            new DataTable('#clientesTable', {
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/1.13.8/i18n/pt-BR.json'
                },
                pageLength: 8,
                lengthChange: false,
                ordering: true,
                info: true,
                searching: true,
                pagingType: "simple_numbers",
                dom:
                    "<'row mb-3'<'col-md-6'f><'col-md-6 text-end'i>>" +
                    "<'table-responsive'tr>" +
                    "<'row mt-3'<'col-md-12 d-flex justify-content-center'p>>"
            });
        });
    </script>


    <style>
        /* Estilos Institucionais DeliFit */
        .text-orange {
            color: #ff6600 !important;
        }

        .bg-light-orange {
            background-color: #fff3e6;
        }

        .btn-orange {
            background-color: #ff6600;
            border: none;
        }

            .btn-orange:hover {
                background-color: #e65c00;
            }

        .btn-orange-outline {
            border: 1px solid #ff6600;
            color: #ff6600;
            background: transparent;
            transition: all 0.2s;
            font-weight: 500;
        }

            .btn-orange-outline:hover {
                background-color: #ff6600;
                color: white;
            }

        .avatar-sm {
            width: 35px;
            height: 35px;
        }

        .table thead th {
            font-size: 0.85rem;
            color: #6c757d;
            border-bottom: 2px solid #f8f9fa;
        }

        .table tbody tr:hover {
            background-color: #fffaf5;
        }

        /* Search */
        .dataTables_filter input {
            border-radius: 20px;
            padding: 6px 14px;
            border: 1px solid #dee2e6;
            outline: none;
        }

            .dataTables_filter input:focus {
                border-color: #ff6600;
                box-shadow: 0 0 0 0.15rem rgba(255, 102, 0, 0.25);
            }

        /* Info */
        .dataTables_info {
            font-size: 0.85rem;
            color: #6c757d;
        }

        /* Paginação */
        .dataTables_paginate .pagination {
            gap: 6px;
        }

        .dataTables_paginate .page-link {
            border-radius: 50%;
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ff6600;
            border: 1px solid #dee2e6;
        }

            .dataTables_paginate .page-link:hover {
                background-color: #fff3e6;
                border-color: #ff6600;
            }

        .dataTables_paginate .page-item.active .page-link {
            background-color: #ff6600;
            border-color: #ff6600;
            color: #fff;
        }

        /* Remove borda feia padrão */
        table.dataTable {
            border-collapse: separate;
            border-spacing: 0;
        }

            /* Hover alinhado com seu layout */
            table.dataTable tbody tr:hover {
                background-color: #fffaf5;
            }

            /* Cabeçalho */
            table.dataTable thead th {
                border-bottom: 2px solid #f8f9fa;
                color: #6c757d;
                font-size: 0.85rem;
            }

    </style>
}
